// Panneau — recettes — écran à construire.
// Écran 8 — tableau, filtres dans l’URL, suppression chiffrée.
// La page arrive avec son propre lot de tests ; ce fichier ne porte pour l'instant que
// sa place dans l'arborescence de routes.

export default function Recettes() {
  return <h1 className="text-3xl">Panneau — recettes</h1>;
}
