// Panneau — catégories — écran à construire.
// Écran 11 — quatre ressources au contrat identique, un seul composant.
// La page arrive avec son propre lot de tests ; ce fichier ne porte pour l'instant que
// sa place dans l'arborescence de routes.

export default function Categories() {
  return <h1 className="text-3xl">Panneau — catégories</h1>;
}
