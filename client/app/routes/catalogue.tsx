// Catalogue — écran à construire.
// Écran 2 — recherche et neuf filtres, panneau collant, pagination.
// La page arrive avec son propre lot de tests ; ce fichier ne porte pour l'instant que
// sa place dans l'arborescence de routes.

export default function Catalogue() {
  return <h1 className="text-3xl">Catalogue</h1>;
}
