// Mon compte — écran à construire.
// Écran 6 — profil, mot de passe, suppression : trois actions séparées.
// La page arrive avec son propre lot de tests ; ce fichier ne porte pour l'instant que
// sa place dans l'arborescence de routes.

export default function MonCompte() {
  return <h1 className="text-3xl">Mon compte</h1>;
}
