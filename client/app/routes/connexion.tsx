// Connexion — écran à construire.
// Écran 4 — un seul libellé d’échec en 401, sans désigner de champ.
// La page arrive avec son propre lot de tests ; ce fichier ne porte pour l'instant que
// sa place dans l'arborescence de routes.

export default function Connexion() {
  return <h1 className="text-3xl">Connexion</h1>;
}
